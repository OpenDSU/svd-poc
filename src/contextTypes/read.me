ContextType is an abstract concept: it can represent:
- "json" contexts that means that SVDs history is executed to obtain a json
- "did" contexts that means that SVDs history is executed to obtain a DID document (W3C standard concept)
- "swarm" context
